package com.feivirus.algorithm.array;

/**
 * @author feivirus
 * 给定一个整数数组(n>1),求把这些整数表示在数轴上时,
 * 相邻两个数差的最大值 (leetcode 164)
 * 先考虑不排序的情况下
 */
public class MaxDistanceBetweenTwoNeighbour {

    public static void main(String[] args) {

    }

    public int maxDistance(int[] values) {
        if (values.length == 1) {
            return  0;
        }
        if (values.length == 2) {

        }

        return 0;
    }

}
