package com.feivirus.algorithm.string;

/**
 * @author feivirus
 * 一个字串只由*号和数字组成，把*号都放到前面.
 * 做法和SortBinaryString一样，从前往后一个指针，从后往前一个指针。
 * 判断是*，交换字符.
 * {@link SortBinaryString}
 */
public class SwapAsterisk {
}
